{
  "name": "adaptance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "displayName": "Adaptance",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "verify": "astro check && astro build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.3.2",
    "typescript": "^6.0.3"
  }
}
